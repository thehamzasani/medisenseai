import type { AssessmentWithResults } from '@/types'

interface ClinicalInsightCardProps {
  assessment: AssessmentWithResults
}

export default function ClinicalInsightCard({ assessment }: ClinicalInsightCardProps) {
  const insight = assessment.clinicalInsight ?? 'No clinical insight available for this assessment.'
  const bestEngine = assessment.bestEngine ?? 'Neural Network'

  return (
    <div className="surface-glass rounded-2xl p-5 flex flex-col gap-4 h-full">
      {/* Header */}
      <div className="flex items-center gap-2">
        <div className="w-8 h-8 rounded-lg bg-primary-fixed-dim/20 flex items-center justify-center">
          <span className="material-symbols-outlined text-primary-fixed-dim" style={{ fontSize: 18 }}>
            auto_awesome
          </span>
        </div>
        <div>
          <p className="text-[10px] uppercase tracking-widest text-primary-fixed-dim font-semibold">
            Clinical Insight
          </p>
          <p className="text-[10px] text-on-surface-variant">AI-generated analysis</p>
        </div>
      </div>

      {/* Divider */}
      <div className="h-px bg-gradient-to-r from-primary-fixed-dim/30 to-transparent" />

      {/* Insight text */}
      <div className="flex-1">
        <div className="relative">
          {/* Decorative quote mark */}
          <span
            className="absolute -top-2 -left-1 text-5xl text-primary-fixed-dim/20 font-serif leading-none select-none"
            aria-hidden
          >
            
          </span>
          <p className="text-body-md text-on-surface leading-relaxed pl-4 pt-2">
            {insight}
          </p>
        </div>
      </div>

      {/* Recommendations preview */}
      {assessment.keyFactors && assessment.keyFactors.length > 0 && (
        <div className="p-3 rounded-xl bg-surface-container-high/50 border border-outline-variant/20">
          <p className="text-[10px] uppercase tracking-wider text-on-surface-variant mb-2">Top Risk Factors</p>
          <ul className="space-y-1">
            {assessment.keyFactors.slice(0, 3).map((factor, i) => (
              <li key={i} className="flex items-start gap-2 text-[11px] text-on-surface">
                <span className="material-symbols-outlined text-error mt-0.5 flex-shrink-0" style={{ fontSize: 12 }}>
                  warning
                </span>
                {factor}
              </li>
            ))}
          </ul>
        </div>
      )}

      {/* Footer */}
      <div className="pt-3 border-t border-outline-variant/20 flex items-center justify-between">
        <div className="flex items-center gap-1.5 text-[10px] text-on-surface-variant">
          <span className="material-symbols-outlined" style={{ fontSize: 12 }}>memory</span>
          <span>Generated by <span className="text-primary-fixed-dim font-semibold">{bestEngine}</span></span>
        </div>
        <div className="flex items-center gap-1 text-[10px] text-tertiary-fixed-dim">
          <span className="material-symbols-outlined" style={{ fontSize: 12 }}>verified</span>
          <span>99.2% accuracy</span>
        </div>
      </div>
    </div>
  )
}